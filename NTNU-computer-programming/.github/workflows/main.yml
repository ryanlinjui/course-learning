name: testing
on:
  #push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - name: test
        uses: rrr20020910/NTNU-computer-programming@main
        run: ls
        